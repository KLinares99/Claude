Small rounded pill tag — labels industry cards, resource-list item types, and inline status chips.

```jsx
<Badge>Churches & Ministries</Badge>
<Badge tone="gold">Español</Badge>
```

`tone="gold"` is reserved for a handful of sparing highlights (Spanish-language resources, the church building-use link) — never the default choice.
